---
id: vfs-001
scope: vfs
status: done
depends-on: [boot-001, abi-001, win-001]
---

# vfs-001: Implement Rootfs Path Model And Fd Table

## Objective

Implement Linux path canonicalization, rootfs jail enforcement, guest inode identity, fd allocation, stdin/stdout/stderr descriptors, close-on-exec flags, and basic fd lookup errors.

## Context

- `docs/architecture/runtime.md`

## Path

- `crates/mcr-vfs/`
- `crates/mcr-win/`

## Verification

```powershell
cargo test -p mcr-vfs
```

## Notes

- Host paths must not become guest inode IDs.
- Include tests for `.`/`..`, symlink loop placeholders, root escape prevention, and fd reuse.
