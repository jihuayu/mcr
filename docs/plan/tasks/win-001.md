---
id: win-001
scope: windows-adapter
status: ready
depends-on: [boot-001, abi-001]
---

# win-001: Implement Windows Host Adapter Skeleton

## Objective

Create `mcr-win` host adapters for memory, files, clocks, random bytes, synchronization, and networking with typed host errors and no guest policy.

## Context

- `docs/architecture/README.md`
- `docs/architecture/runtime.md`

## Path

- `crates/mcr-win/`

## Verification

```powershell
cargo test -p mcr-win
```

## Notes

- Linux errno conversion remains above this crate.
- Non-Windows builds may use compile-time stubs for unit tests, but Windows APIs must be the production path.
