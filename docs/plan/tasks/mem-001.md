---
id: mem-001
scope: memory
status: pending
depends-on: [elf-002, sys-001, win-001]
---

# mem-001: Implement Guest Memory Syscalls

## Objective

Implement MVP memory syscall behavior for `mmap`, `munmap`, `mprotect`, and `brk` against the guest VMA model and Windows memory adapter.

## Context

- `docs/architecture/runtime.md`

## Path

- `crates/mcr-runtime/`
- `crates/mcr-sys/`
- `crates/mcr-win/`

## Verification

```powershell
cargo test -p mcr-runtime
cargo test -p mcr-sys
```

## Notes

- Include overlap, permission, unmap, invalid address, and brk growth/shrink tests.
- File-backed mmap coherence can be minimal for MVP and documented as constrained.
