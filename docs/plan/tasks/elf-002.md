---
id: elf-002
scope: elf
status: done
depends-on: [elf-001, win-001]
---

# elf-002: Build Guest Memory Map And Initial Stack

## Objective

Map ELF load plans into guest memory, register VMAs, and construct Linux initial stack data for `argc`, `argv`, `envp`, and MVP auxv entries.

## Context

- `docs/architecture/runtime.md`

## Path

- `crates/mcr-elf/`
- `crates/mcr-runtime/`
- `crates/mcr-win/`

## Verification

```powershell
cargo test -p mcr-elf
cargo test -p mcr-runtime
```

## Notes

- Include deterministic tests for stack byte layout and auxv values.
- Dynamic linker execution may remain incomplete until `elf-003`.
