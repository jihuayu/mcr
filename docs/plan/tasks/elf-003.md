---
id: elf-003
scope: elf
status: done
depends-on: [integ-001]
---

# elf-003: Complete Dynamic Alpine Execution Path

## Objective

Support the dynamic linker path needed by Alpine shell and common language runtime binaries, including interpreter loading and auxv entries required by musl.

## Context

- `docs/architecture/runtime.md`
- `docs/development/README.md`

## Path

- `crates/mcr-elf/`
- `crates/mcr-runtime/`
- `crates/mcr-vfs/`
- `crates/mcr-testkit/`

## Verification

```powershell
cargo test -p mcr-elf
cargo test -p mcr-runtime
```

## Notes

- Keep dynamic linking support scoped to Alpine/musl requirements for Phase 2.
- Dynamic execution must leave musl TLS and FS-base setup compatible with the `ARCH_SET_FS`/`ARCH_GET_FS` contract.
- glibc-specific gaps may be documented if not needed by Phase 2 smoke tests.
