---
id: fd-001
scope: fd
status: pending
depends-on: [integ-001, vfs-003]
---

# fd-001: Implement Pipes, Duplication, Fcntl, And Ioctl Subset

## Objective

Implement `pipe`, `pipe2`, `dup`, `dup2`, `dup3`, common `fcntl` operations, and selected `ioctl` behavior required by shell, stdio, and non-interactive tooling.

## Context

- `docs/architecture/runtime.md`

## Path

- `crates/mcr-vfs/`
- `crates/mcr-sys/`
- `crates/mcr-runtime/`

## Verification

```powershell
cargo test -p mcr-vfs
cargo test -p mcr-sys
```

## Notes

- Cover blocking read/write behavior enough for shell pipelines.
- TTY/PTY completeness is not part of this task.
