---
id: net-002
scope: network
status: pending
depends-on: [net-001]
---

# net-002: Implement Poll And Epoll Readiness

## Objective

Implement `poll`, `ppoll`, `epoll_create1`, `epoll_ctl`, and `epoll_wait` over a shared runtime readiness queue for sockets, pipes, and internal wakeups.

## Context

- `docs/architecture/runtime.md`

## Path

- `crates/mcr-net/`
- `crates/mcr-vfs/`
- `crates/mcr-sys/`
- `crates/mcr-runtime/`

## Verification

```powershell
cargo test -p mcr-net
cargo test -p mcr-vfs
mcr run-rootfs alpine-rootfs /bin/sh -c "curl -fsSL https://example.com >/dev/null"
```

## Notes

- Use Winsock/`WSAPoll` first; IOCP optimization is deferred.
- Include timeout, removed watch, closed fd, pipe readiness, and socket readiness tests.
- The HTTPS smoke must exercise readiness through the guest shell path.
