---
id: vfs-004
scope: vfs
status: pending
depends-on: [task-002, vfs-003]
---

# vfs-004: Implement Minimal Procfs And Devfs

## Objective

Implement `/proc/self/exe`, `/proc/self/cmdline`, `/proc/self/environ`, `/proc/self/fd`, `/proc/self/fd/<n>`, `/dev/null`, `/dev/zero`, and `/dev/urandom`.

## Context

- `docs/architecture/runtime.md`

## Path

- `crates/mcr-vfs/`
- `crates/mcr-task/`
- `crates/mcr-sys/`
- `crates/mcr-runtime/`

## Verification

```powershell
cargo test -p mcr-vfs
cargo test -p mcr-task
```

## Notes

- `/proc/self/fd/<n>` must report guest fd targets, not host handles.
- Full `/proc`, `/sys`, and tty/pty are outside this task.
