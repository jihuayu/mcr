---
id: vfs-003
scope: vfs
status: done
depends-on: [integ-001]
---

# vfs-003: Add Writable VFS Mutation Semantics

## Objective

Implement writable rootfs operations needed by shell and package-manager setup: `mkdirat`, `unlinkat`, `renameat2`, `symlinkat`, `linkat`, `ftruncate`, `getcwd`, `chdir`, `umask`, metadata sidecar, and delayed unlink behavior.

## Context

- `docs/architecture/runtime.md`

## Path

- `crates/mcr-vfs/`
- `crates/mcr-sys/`
- `crates/mcr-runtime/`

## Verification

```powershell
cargo test -p mcr-vfs
```

## Notes

- Include rename-over-existing, unlink-open-file, symlink, hardlink, cwd, and metadata tests.
- Overlay export and OCI whiteout remain deferred.
